{% set path = app.request.pathInfo %}

<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
     <li class="nav-item {% if path == path('admin_home') %} active {% endif %}">
        <a class="nav-link" href="{{ path('admin_home') }}">Accueil</a>
      </li>
      <li class="nav-item {% if path == path('admin_projects') %} active {% endif %}">
        <a class="nav-link" href="{{ path('admin_projects') }}">Gestion des projets</a>
      </li>
      <li class="nav-item {% if path == path('admin_infos_add') %} active {% endif %}">
        <a class="nav-link" href="{{ path('admin_infos_add') }}">Informations personnelles</a>
      </li>
            <li class="nav-item {% if path == path('admin_infos_add') %} active {% endif %}">
        <a class="nav-link" href="{{ path('security_logout') }}">Déconnexion</a>
      </li>

    </ul>
  </div>
  <a target="_blank" class="btn btn-success" href="/">Voir le site</a>
</nav>